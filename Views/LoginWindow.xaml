<Window x:Class="ExcelMaker.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ExcelMaker — 登录" Height="320" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border Background="#2B2B2B" CornerRadius="0">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="ExcelMaker" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>
            <TextBlock Grid.Row="0" Text="库存数据处理工具" Foreground="#999" FontSize="12" HorizontalAlignment="Right" Margin="0,5,0,0"/>

            <StackPanel Grid.Row="1" Margin="0,0,0,10">
                <TextBlock Text="账号" Foreground="#CCC" FontSize="12" Margin="0,0,0,4"/>
                <TextBox x:Name="UsernameBox" Height="32" FontSize="14" Padding="6,4"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Margin="0,0,0,10">
                <TextBlock Text="密码" Foreground="#CCC" FontSize="12" Margin="0,0,0,4"/>
                <PasswordBox x:Name="PasswordBox" Height="32" FontSize="14" Padding="6,4"/>
            </StackPanel>

            <CheckBox Grid.Row="3" x:Name="RememberCheckBox" Content="记住密码" Foreground="#CCC"
                      IsChecked="{Binding RememberPassword}" Margin="0,5" Unchecked="RememberCheckBox_Unchecked"/>

            <StackPanel Grid.Row="4" Margin="0,10,0,0">
                <TextBlock Text="{Binding ErrorMessage}" Foreground="#FF6B6B" FontSize="12" Margin="0,0,0,8"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
                <Button Content="登 录" Height="36" FontSize="15"
                        Style="{StaticResource BtnPrimary}"
                        Command="{Binding LoginCommand}" IsEnabled="{Binding LoginCommand.CanExecute}"/>
            </StackPanel>

            <Button Grid.Row="5" Content="✕" Width="32" Height="32" HorizontalAlignment="Right" VerticalAlignment="Top"
                    Background="Transparent" Foreground="#999" BorderThickness="0" FontSize="16"
                    Click="CloseButton_Click" Margin="0,-15,-15,0"/>
        </Grid>
    </Border>
</Window>
